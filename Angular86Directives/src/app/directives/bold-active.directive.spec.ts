import { BoldActiveDirective } from './bold-active.directive';

describe('BoldActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldActiveDirective();
    expect(directive).toBeTruthy();
  });
});
