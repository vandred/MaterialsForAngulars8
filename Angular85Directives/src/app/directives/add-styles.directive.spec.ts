import { AddStylesDirective } from './add-styles.directive';

describe('AddStylesDirective', () => {
  it('should create an instance', () => {
    const directive = new AddStylesDirective();
    expect(directive).toBeTruthy();
  });
});
