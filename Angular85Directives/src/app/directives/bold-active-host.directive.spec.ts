import { BoldActiveHostDirective } from './bold-active-host.directive';

describe('BoldActiveHostDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldActiveHostDirective();
    expect(directive).toBeTruthy();
  });
});
