import { BoldInputDirective } from './bold-input.directive';

describe('BoldInputDirective', () => {
  it('should create an instance', () => {
    const directive = new BoldInputDirective();
    expect(directive).toBeTruthy();
  });
});
