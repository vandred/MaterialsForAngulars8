import { BoldActive2Directive } from './bold-active2.directive';

describe('BoldActive2Directive', () => {
  it('should create an instance', () => {
    const directive = new BoldActive2Directive();
    expect(directive).toBeTruthy();
  });
});
